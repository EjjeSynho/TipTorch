#%%
import os
import torch
import numpy as np
from astropy.io import fits
from os import path

data_path = 'C:/Users/akuznets/Data/SPHERE/SPHERE_DC_DATA/'
folders = os.listdir(data_path)


#%%
fits_files = []
for folder in folders:
    files = os.listdir(path.join(data_path,folder))
    for file in files:
        fits_files.append(path.join(data_path,folder,file))

#%%

id = 256

#with fits.open(fits_files[id]) as hdul:
    #hdul.info()
    #obs_info = hdul[0].header
hdul = fits.open(fits_files[id])
test = hdul[0].header